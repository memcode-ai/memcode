{
  "_comment": "Operational config: which model from the catalog (models.json) plays which ROLE. This is the swap knob — change a role's model id here + redeploy. The roles ride /v1/models so the CLI's selection policy maps its role lanes onto them; the model's properties come from models.json. (The old fireworks_tier moved into models.json `tiers` — tier triples are catalog data now.)",

  "roles": {
    "planner": "accounts/fireworks/models/glm-5p2",
    "reviewer": "gpt-5.6-luna",
    "standard": "accounts/fireworks/models/glm-5p2",
    "classify": "accounts/fireworks/models/gpt-oss-120b"
  }
}
